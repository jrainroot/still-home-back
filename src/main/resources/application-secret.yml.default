# application-secret.yml 에 이런 정보가 들어간다는걸 팀원에게 알리는 용도

spring:
  security:
    oauth2:
      client:
        registration:
          kakao:
            client-id: CLIENT_ID
#  email 전송정보
  mail:
    password: NEET_TO_INPUT
custom:
  security:
    oauth2:
      client:
        registration:
          kakao:
            devUser:
              oauthId: oauthId
              nickname: nickname